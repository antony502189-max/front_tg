services:
  - type: web
    name: bsuir-nexus-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python -m backend.server
    healthCheckPath: /api/health
    envVars:
      - key: HOST
        value: 0.0.0.0
